---
#define host
- hosts: "{{hostname}}"
  gather_facts: yes
  become: yes
    tasks:
  - name: ping
    action: ping
    tags: "check-ping"
  roles:
    - role: ansible-role-nginx
      vars:
        ansible_python_interpreter: "/usr/bin/python3" 